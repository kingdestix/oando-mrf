PORT=5000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_NAME=oando_mrf
DB_USER=postgres
DB_PASSWORD=CHANGE_THIS_PASSWORD

JWT_SECRET=GENERATE_A_NEW_SECRET_USING_node_-e_"console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_EXPIRE=7d

UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=GENERATE_GMAIL_APP_PASSWORD
EMAIL_FROM="Procurement Oando <your_email@gmail.com>"

CORS_ORIGIN=http://localhost:5000
